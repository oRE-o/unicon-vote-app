name: Manual Ops Note

on:
  workflow_dispatch:

jobs:
  deployment-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current deployment flow
        run: |
          echo "Push-based CI/CD is disabled for this repository."
          echo "Run updates directly on the target server instead."
          echo ""
          echo "Recommended server update flow:"
          echo "  cd ~/unicon-vote-app"
          echo "  univote update"
